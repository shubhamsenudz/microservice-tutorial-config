eureka:
  instance:
    prefer-ip-address:  true
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8762/eureka/
    instance:
    hostname: localhost
    prefer-ip-address: true
spring:
  application:
    name: Config-Default
    
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
