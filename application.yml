eureka:
  instance:
    prefer-ip-address:  true
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${http://localhost:8762/eureka}
spring:
  application:
    name: CONFIG-SERVER
    
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
